/*
 * WatchTrailerSection Messages
 *
 * This contains all the text for the WatchTrailerSection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WatchTrailerSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Watch The Trailer',
  },
});
